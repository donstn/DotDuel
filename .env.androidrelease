# Loaded ONLY by `npm run build:android-release` (vite --mode androidrelease).
# Flips AdMob from the test unit to the real banner unit for the Play .aab.
# NEVER set this in .env / .env.local — emulator runs would hit the real unit.
VITE_ADMOB_REAL=1
