# Supabase client config (public — Row-Level Security enforces access).
# Dashboard → Settings → API. Copy this file to .env.local and fill in.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
